<!DOCTYPE html>
<html>
  <head>
    <title>CastApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header>
      <div class="topbar">
        <div class="logo">
          <h1>Cast Group Teste</h1>
        </div>
        <ul class="nav-links">
          <li>
            <%= link_to  'Registre-se', new_user_registration_path %>
          </li>
          <li>
            <%= link_to 'Login', new_user_session_path unless user_signed_in? %>
            <%= link_to 'Sair', destroy_user_session_path, data: { "turbo-method": :delete } if user_signed_in? %>
          </li>
        </ul>
      </div>
      <nav class="navbar">
        <ul>
          <li><%= link_to 'Pagina inicial', root_path %></li>
          <li><%= link_to 'Dashboard', dashboard_path %></li>
          <li><%= link_to 'Contato', new_contacts_path %></li>
        </ul>
      </nav>
    </header>

    <div class="content">
      <%= yield %>
    </div>

    <footer>
      <p>Edivan de Castro - © 2024 Cast Group</p>
    </footer>
  </body>
</html>
